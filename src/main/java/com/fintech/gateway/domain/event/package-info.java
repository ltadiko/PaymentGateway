/**
 * Domain events — sealed interface with records.
 * Represent significant state changes in the payment lifecycle.
 * Carry only IDs, no sensitive data (PCI compliance).
 */
package com.fintech.gateway.domain.event;

