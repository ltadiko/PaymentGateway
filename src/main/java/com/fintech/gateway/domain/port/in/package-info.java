/**
 * Inbound ports — use case interfaces that the application layer implements.
 * Called by infrastructure adapters (REST controllers, Kafka consumers).
 */
package com.fintech.gateway.domain.port.in;

